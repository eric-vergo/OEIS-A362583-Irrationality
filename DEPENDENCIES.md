# DEPENDENCIES.md — Mathlib dependency audit (spec §4)

Pinned toolchain: `leanprover/lean4:v4.31.0`.
Pinned Mathlib: tag `v4.31.0` (rev `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f`).

Status legend: **MWE-verified** = declaration confirmed at the pin AND exercised by a
compiling minimal working example in `Audit/`. **absent (confirmed)** = checked missing
at the pin; workaround recorded.

Audit completed 2026-07-03 (Phase 0c). Every row M1–M13 has a compiling, `sorry`-free
MWE in `Audit/M<nn>.lean`; M14 is an absence check (no file).

| Item | Ingredient | Verified Mathlib name(s) | Module | Status | Notes |
|---|---|---|---|---|---|
| M1 | χ₄ as a character; value lemmas; bridge to ℂ | `ZMod.χ₄ : MulChar (ZMod 4) ℤ`; `ZMod.χ₄_nat_eq_if_mod_four`, `ZMod.χ₄_nat_one_mod_four`, `ZMod.χ₄_nat_three_mod_four` (`{n}` implicit, hyp `n % 4 = _`); `MulChar.ringHomComp`, `MulChar.ringHomComp_apply`, `MulChar.ringHomComp_ne_one_iff` | `Mathlib.NumberTheory.LegendreSymbol.ZModChar`, `Mathlib.NumberTheory.MulChar.Basic` | MWE-verified (`Audit/M01.lean`) | Project character: `def χ : DirichletCharacter ℂ 4 := ZMod.χ₄.ringHomComp (Int.castRingHom ℂ)`. `χ ≠ 1` proved via evaluation at 3 + `MulChar.one_apply`; feed injectivity as `(Int.castRingHom ℂ).injective_int`. Value computation pattern: `unfold χ; rw [MulChar.ringHomComp_apply, χ₄_nat_three_mod_four (by norm_num)]; norm_num`. Rename trap: `isUnit_of_mul_eq_one` → `IsUnit.of_mul_eq_one`. |
| M2 | Continued L-function; entire for nonprincipal; = LSeries on Re s > 1 | `DirichletCharacter.LFunction`; `DirichletCharacter.differentiable_LFunction` (**χ implicit**) `(hχ : χ ≠ 1)`; `DirichletCharacter.LFunction_eq_LSeries` (**χ explicit**, `{s}` implicit, `hs : 1 < re s`) | `Mathlib.NumberTheory.LSeries.DirichletContinuation` | MWE-verified (`Audit/M02.lean`) | RHS of `LFunction_eq_LSeries` is `LSeries (fun n : ℕ ↦ χ n) s` (not the `L ↗χ` notation). `NeZero 4` synthesizes automatically. |
| M3 | L(1,χ) ≠ 0 for nonprincipal χ | `DirichletCharacter.LFunction_apply_one_ne_zero` (χ implicit, `hχ : χ ≠ 1`); `LFunction_ne_zero_of_re_eq_one` (χ explicit; hs **before** hχs); `LFunction_ne_zero_of_one_le_re` (χ explicit, strict-implicit `⦃s⦄`, hχs **before** hs) | `Mathlib.NumberTheory.LSeries.Nonvanishing` | MWE-verified (`Audit/M03.lean`) | The two stronger variants have opposite hypothesis orders — mind the binders. |
| M4 | Euler product, exp form (**risk R2 — retired**) | `DirichletCharacter.LSeries_eulerProduct_exp_log χ hs : exp (∑' p : Nat.Primes, -log (1 - χ p * p ^ (-s))) = L ↗χ s` for `1 < s.re` | `Mathlib.NumberTheory.EulerProduct.DirichletLSeries` | MWE-verified (`Audit/M04.lean`) | Closes for our χ as a **one-line `exact`** stating the tsum naively. Elaboration: `χ ↑↑p` = `χ ((p.val : ℕ) : ZMod 4)` (Subtype.val then Nat.cast; no direct Primes→ZMod coe); `↑↑p ^ (-s)` is **cpow with ℕ-cast-to-ℂ base**. Chain to the continued function: `rw [LFunction_eq_LSeries χ hs]; exact LSeries_eulerProduct_exp_log χ hs` (verified). Fallback (`exp_tsum_primes_log_eq_tsum` + `summable_dirichletSummand`) noted in file but NOT needed. |
| M5 | ∑ 1/p = ∞ | `Nat.Primes.not_summable_one_div`; `not_summable_one_div_on_primes` (root ns, indicator form); `Nat.Primes.summable_rpow : Summable (fun p : Nat.Primes ↦ (p:ℝ) ^ r) ↔ r < -1` | `Mathlib.NumberTheory.SumPrimeReciprocals` | MWE-verified (`Audit/M05.lean`) | Term-mode, no tactics needed. |
| M6 | Infinitely many primes ≡ 1 and ≡ 3 (mod 4) | `Nat.infinite_setOf_prime_and_eq_mod (ha : IsUnit a) : {p : ℕ \| p.Prime ∧ (p : ZMod q) = a}.Infinite`; `Nat.forall_exists_prime_gt_and_eq_mod` | `Mathlib.NumberTheory.LSeries.PrimesInAP` | MWE-verified (`Audit/M06.lean`) | Both classes at q=4: `IsUnit (1/3 : ZMod 4)` by `isUnit_one` / `by decide`. **ZMod↔% bridge** (needed project-wide): `(p : ZMod 4) = 3 ↔ p % 4 = 3` via `rw [← Nat.cast_ofNat (R := ZMod 4) (n := 3), ZMod.natCast_eq_natCast_iff']` (use `← Nat.cast_one` for class 1); ModEq variant `ZMod.natCast_eq_natCast_iff`. No elementary 3-mod-4 lemma in Mathlib — general theorem it is. |
| M7 | `Nat.nth` / `Nat.count` prime-indexing | `Nat.count p n` strict (< n); `Nat.primeCounting n` = # primes **≤ n** = `count Prime (n+1)`; `Nat.nth_count`, `Nat.count_nth`, `Nat.count_nth_of_infinite`, `Nat.nth_lt_nth` (Infinite hyp explicit), `Nat.count_le_iff_le_nth`, `Nat.primeCounting'_nth_eq`; **pins pre-exist as simp lemmas**: `Nat.nth_prime_zero_eq_two`, `Nat.nth_prime_one_eq_three`, `Nat.nth_prime_two_eq_five` (`Mathlib.Data.Nat.Prime.Nth`) | `Mathlib.Data.Nat.Count`, `Mathlib.Data.Nat.Nth`, `Mathlib.NumberTheory.PrimeCounting`, `Mathlib.Data.Nat.Prime.Nth` | MWE-verified (`Audit/M07.lean`) | `Nat.nth Nat.Prime 0 = 2 := Nat.nth_count Nat.prime_two` (count reduces definitionally). `count` small values: `by decide` (NOT `simp [Nat.count_succ]`). `primeCounting 10 = 4` by `rfl` or `decide`. Finiteness hyp discharge: `Nat.count_nth_of_infinite Nat.infinite_setOf_prime`. Trap: `norm_num` can't prove `Nat.Prime 2` without `Mathlib.Tactic.NormNum.Prime` import. |
| M8 | Identity theorem + half-plane topology | `AnalyticOnNhd.eqOn_of_preconnected_of_eventuallyEq` (`z₀` implicit, inferred from `h₀`); entry `DifferentiableOn.analyticOnNhd (hd) (hs : IsOpen s)`; **(a)** Ω open: `isOpen_lt continuous_const Complex.continuous_re`; **(b)** Ω preconnected: `(convex_halfSpace_re_gt r).isPreconnected` (**root namespace**, r explicit); **(c)** eventuallyEq: `Filter.eventuallyEq_of_mem (hO.mem_nhds h₂O) hfg`; **(d)** membership: `norm_num [Set.mem_setOf_eq]` | `Mathlib.Analysis.Analytic.Uniqueness`, `Mathlib.Analysis.Complex.CauchyIntegral`, `Mathlib.Analysis.Complex.Convex`, `Mathlib.Analysis.Convex.PathConnected` | MWE-verified (`Audit/M08.lean`) | Full D3 skeleton compiles end-to-end: f, g holomorphic on Ω = {1/2 < re}, agreeing on {1 < re} ⇒ EqOn on Ω. No dedicated half-space-open lemma exists; `isOpen_lt` route is canonical. |
| M9 | Locally uniform limits of holomorphic are holomorphic | `TendstoLocallyUniformlyOn.differentiableOn` (`[CompleteSpace E] [φ.NeBot]`); `TendstoLocallyUniformlyOn.deriv`; `Complex.differentiableOn_tsum_of_summable_norm` (**Complex namespace**, unlike neighbors) | `Mathlib.Analysis.Complex.LocallyUniformLimit` | MWE-verified (`Audit/M09.lean`) | Partial-sum instantiation with `φ = atTop` works; eventual hyp via `Filter.Eventually.of_forall` + `DifferentiableOn.fun_sum`. `differentiableOn_tsum_of_summable_norm` does NOT shortcut N1 (needs one summable bound uniform on U; C/n^δ isn't summable for δ ≤ 1) — bounded-partial-sums route through M9 stands. |
| M10 | Summation by parts (discrete + integral) | Discrete: `Finset.sum_Ioc_by_parts` (hyp `m < n`), `sum_Ico_by_parts`, `sum_range_by_parts` — `•` over Module, `simpa only [smul_eq_mul]` for ℝ. Integral: `sum_mul_eq_sub_sub_integral_mul` (+`₀`,`₁`, primed ℕ-endpoint variants), `tendsto_sum_mul_atTop_nhds_one_sub_integral(₀)`, `summable_mul_of_bigO_atTop(')` | `Mathlib.Algebra.BigOperators.Module`, `Mathlib.NumberTheory.AbelSummation` | MWE-verified (`Audit/M10.lean`) | **Best fit for weight y^(−s) against prime-indicator coefficients: `sum_mul_eq_sub_integral_mul₁`** (needs `c 0 = c 1 = 0` — true for prime indicators; everything on `Icc 2 b`, no singularity at 0). Integral-form side conditions: DifferentiableAt on Icc, `IntegrableOn (deriv f)`, `⌊·⌋₊` plumbing. Discrete form: no analytic side conditions but hand-bounding of `f(i+1) − f i` (→ M12). |
| M11 | `Int.fract` API + denominator fact | `Int.fract_add_intCast`, `fract_intCast_add`, `fract_add_natCast`, `fract_add_one`, `fract_eq_self`, `fract_nonneg`, `fract_lt_one`, `fract_fract`, `fract_add`; **denominator fact EXISTS**: `Int.fract_div_intCast_eq_div_intCast_mod {m : ℤ} {n : ℕ} : fract ((m:k)/n) = ((m % n : ℤ):k)/n` (+ ℕ twin) | `Mathlib.Algebra.Order.Floor.Ring` | MWE-verified (`Audit/M11.lean`) | Name trap: `*Cast`-suffixed at this pin — `Int.fract_add_int` does NOT exist (no deprecated alias). `fract(2^j·(a/b)) = ((2^j·a) % b)/b` is casting glue only — **no new lemma needed for B4**. Pigeonhole MWE `fract (2^4·(3/7)) = 6/7` passes. |
| M12 | ‖(n:ℂ)^(−s)‖ norms; increment bound | `Complex.norm_natCast_cpow_of_pos (hn : 0 < n)`, `norm_natCast_cpow_of_re_ne_zero`, `norm_cpow_eq_rpow_re_of_pos`, `norm_cpow_eq_rpow_re_of_nonneg`, `norm_natCast_cpow_pos_of_pos` | `Mathlib.Analysis.SpecialFunctions.Pow.Real` | MWE-verified (`Audit/M12.lean`); increment bound **absent (confirmed)** | Increment bound `‖n^{−s} − (n+1)^{−s}‖ ≤ ‖s‖ n^{−σ−1}` absent (grep + 2 Loogle queries). Verified FTC route: `intervalIntegral.integral_eq_sub_of_hasDerivAt` + `hasDerivAt_ofReal_cpow_const (hx : x ≠ 0) (hr : r ≠ 0)` + `Real.rpow_le_rpow_of_nonpos` + `intervalIntegral.norm_integral_le_of_norm_le_const`. Real analogue: `Real.hasDerivAt_rpow_const` / `Real.deriv_rpow_const`. Rewrite trap: rewrite `(2:ℂ) → ((2:ℕ):ℂ)` in the lemma instance, not the goal (hits the exponent's 2 otherwise). |
| M13 | tsum toolkit | Regroup: `Summable.tsum_prod`, `tsum_prod'`, `tsum_sigma`, `tsum_comm` (ℝ≥0∞-free versions need summability on total type). Peel: `Summable.sum_add_tsum_nat_add`, `Summable.tsum_eq_zero_add`, `summable_nat_add_iff`. Compare: `Summable.of_norm_bounded` (**g implicit at this pin**, hg first), `Summable.of_nonneg_of_le`, `of_norm_bounded_eventually_nat`. Geometric: `tsum_geometric_of_lt_one`, `tsum_geometric_of_norm_lt_one`, `tsum_geometric_le_of_norm_lt_one` | `Mathlib.Topology.Algebra.InfiniteSum.*`, `Mathlib.Analysis.SpecificLimits.*` | MWE-verified (`Audit/M13.lean`) | **T(s) split incantation (verified):** `rw [← hf.sum_add_tsum_nat_add 3, Finset.sum_range_succ, Finset.sum_range_succ, Finset.sum_range_one]` gives `∑' k, f k = f 0 + f 1 + f 2 + ∑' k, f (k+3)`. |
| M14 | Rational ⟺ eventually periodic binary digits | **ABSENT (confirmed)** — no "eventually periodic" notion anywhere in Mathlib; 0/151 `Irrational` declarations mention periodicity. **But** real-digit infrastructure EXISTS: `Real.digits x b`, `Real.ofDigits`, round-trip `Real.ofDigits_digits (hb : 1 < b) (hx : x ∈ Set.Ico 0 1)`, shift identity `ofDigits_eq_sum_add_ofDigits`, `summable_ofDigitsTerm` | `Mathlib.Analysis.Real.OfDigits` (2025 addition); `Irrational` in `Mathlib.NumberTheory.Real.Irrational` | absence confirmed; N2 proceeds | Step-B extraction is one lemma: `exists_rat_of_not_irrational : ¬Irrational x → ∃ q : ℚ, x = q`; denominator via `q.den_pos` + `Rat.cast_def`. Reverse direction helpers: `Rat.not_irrational`, `irrational_iff_ne_rational`. Phase 2 should consider building tails on `Real.ofDigits` machinery rather than fully bespoke. |
| M15 | Chebyshev-type π lower bound | — | — | not needed | Deliberately avoided by the §2 route. |
